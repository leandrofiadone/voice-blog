---
import Layout from "../layouts/Layout.astro"
import VoiceRecorder from "../components/VoiceRecorder.jsx"
---

<Layout title="Mi Blog con Dictado por Voz">
  <!-- Hero Section -->
  <section
    class="relative bg-gradient-to-r from-blue-500 to-indigo-600 text-white py-16 px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl mx-auto text-center">
      <h1 class="text-4xl sm:text-5xl font-bold mb-4 tracking-tight">
        Mi Blog Personal
      </h1>
      <p class="text-lg sm:text-xl text-blue-100">
        Crea y comparte contenido único usando tu voz y la potencia de la IA
      </p>
    </div>
  </section>

  <!-- Voice Recorder Section -->
  <section class="max-w-3xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
    <div
      class="bg-white rounded-xl shadow-xl p-8 mb-12 transition-all duration-300 hover:shadow-2xl">
      <h2 class="text-2xl sm:text-3xl font-semibold text-gray-800 mb-6">
        Crear Nueva Entrada
      </h2>
      <VoiceRecorder client:load />
    </div>
  </section>

  <!-- Blog Entries Section -->
  <section class="max-w-4xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
    <h2 class="text-2xl sm:text-3xl font-semibold text-gray-800 mb-8">
      Últimas Entradas
    </h2>
    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
      <!-- Sample Blog Post Card -->
      <article
        class="bg-white rounded-lg shadow-md overflow-hidden transition-transform duration-300 hover:scale-105">
        <div class="p-6">
          <h3 class="text-xl font-semibold text-gray-800 mb-2">
            Mi Primera Entrada
          </h3>
          <p class="text-gray-600 text-sm mb-4">
            Esta es una entrada de ejemplo creada con mi voz. ¡Explora cómo la
            IA transforma mis ideas en texto!
          </p>
          <div class="flex items-center text-sm text-gray-500">
            <svg
              class="w-4 h-4 mr-1"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
              xmlns="http://www.w3.org/2000/svg">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
              ></path>
            </svg>
            <span>14 de Julio, 2025</span>
          </div>
        </div>
        <a
          href="#"
          class="block bg-blue-500 text-white text-center py-3 font-medium hover:bg-blue-600 transition-colors"
          aria-label="Leer entrada completa">Leer Más</a
        >
      </article>
      <!-- Add more articles as needed -->
    </div>
  </section>
</Layout>
